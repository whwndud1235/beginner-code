#include <stdio.h>

int main()5 
{
    int i,j, max, N;
    scanf("%d %d", &N, &max);

    int card[N];
    int sum = 0;
    for (i = 0; i < N; i++)
    {
        scanf("%d", &card[i]);
    }
    
    for(i=0;i<N;i++)
    {
        for(j=i+1;j<N;j++)
        {
            card[i]
        }
    }




    for (i = 0; i < N; i++)
    {
        printf("%d ", card[i]);
    }
}