#include <stdio.h>

int main()
{
    int n, i, j;

    scanf("%d", &n);

    int x[n];
    int y[n];
    for (i = 0; i < n; i++)
    {
        scanf("%d %d", &x[i], &y[i]);
    }

 
    for(i=0;i<n;i++)
    {

    }

    for (i = 0; i < n; i++)
    {
        printf("%d %d\n", x[i], y[i]);
    }
}
